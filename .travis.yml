language: java
jdk: openjdk11
sudo: true

before_install:
  - chmod +x ./scripts/install-dependencies.sh

install:
  - ./scripts/install-dependencies.sh

script:
  - cd buildinggame
  - mvn clean install